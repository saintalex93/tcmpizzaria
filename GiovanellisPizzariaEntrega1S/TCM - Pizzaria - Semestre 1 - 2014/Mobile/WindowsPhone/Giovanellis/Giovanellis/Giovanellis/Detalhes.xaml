<phone:PhoneApplicationPage 
    x:Class="Giovanellis.Detalhes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True" Loaded="PhoneApplicationPage_Loaded">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Style="{StaticResource LayoutGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="PageTitle" Text="Detalhes" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <TextBlock Height="30" HorizontalAlignment="Center" Margin="0,6,0,0" Name="txtEndereco" Text="Endereço" VerticalAlignment="Top" TextAlignment="Center" />
            <TextBlock Height="Auto" HorizontalAlignment="Center" Margin="0,42,0,0" Name="txtNome" Text="Nome" VerticalAlignment="Top" FontSize="25" />
            <TextBlock Height="30" HorizontalAlignment="Center" Margin="0,82,0,0" Name="txtHoraValor" Text="Hora - ValorPedido" VerticalAlignment="Top" />
            <TextBlock Height="Auto" HorizontalAlignment="Center" Margin="0,133,0,0" Name="textBlock3" Text="Pagamento" VerticalAlignment="Top" FontSize="30" />
            <TextBlock Height="30" HorizontalAlignment="Center" Margin="0,179,0,0" Name="txtForma" Text="Forma" VerticalAlignment="Top" />
            <TextBlock Height="30" HorizontalAlignment="Center" Margin="0,229,0,0" Name="txtQtdeProdutos" Text="QtdeProdutos" VerticalAlignment="Top" FontSize="25"/>
            <ListBox 
                Height="290" 
                HorizontalAlignment="Stretch" 
                Margin="0,265,0,0" 
                Name="lstProdutos" 
                VerticalAlignment="Top" 
                Width="Auto">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock  
                            HorizontalAlignment="Center" 
                            TextAlignment="Center" 
                            Text="{Binding}" 
                            FontSize="20" 
                            Margin="0" 
                            Style="{StaticResource PhoneTextNormalStyle}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Ligar celular" Height="72" HorizontalAlignment="Left" Margin="0,556,0,0" Name="btnCelular" VerticalAlignment="Top" Width="Auto" Click="btnCelular_Click" />
            <Button Content="Ligar residência" Height="72" HorizontalAlignment="Right" Margin="0,556,0,0" Name="btnResidencia" VerticalAlignment="Top" Width="Auto" Click="btnResidencia_Click" />
        </Grid>
    </Grid>
 
    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>
